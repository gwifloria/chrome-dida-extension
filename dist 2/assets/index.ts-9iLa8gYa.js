chrome.runtime.onInstalled.addListener(()=>{console.log("滴答清单 New Tab 扩展已安装")});chrome.alarms.create("refreshToken",{periodInMinutes:30});chrome.alarms.onAlarm.addListener(async t=>{if(t.name==="refreshToken"){const e=(await chrome.storage.local.get("auth_token")).auth_token;if(e&&e.expires_at&&Date.now()>e.expires_at-6e5&&e.refresh_token)try{chrome.runtime.sendMessage({type:"REFRESH_TOKEN"})}catch{}}});
